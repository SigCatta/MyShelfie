package it.polimi.ingsw.model.board;

public class RefreshTrigger {
    public static boolean isBoardRefreshable(Board board){
        //TODO check if the board has only isolated tiles
        return true;
    }
}
